{
  "name": "vadgr-cua-extension",
  "version": "0.7.7",
  "private": true,
  "description": "vadgr-computer-use browser tier - MV3 extension (DOM-first, native messaging)",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "0.25.0",
    "happy-dom": "^20.14.3",
    "typescript": "^5.4.0",
    "vite": "^7.3.6",
    "vitest": "^4.1.11"
  }
}
